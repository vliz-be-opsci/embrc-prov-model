<!-- Observing activity --> 
<methods>
    <methodStep id="{{identifier of the observering activity}}">
        <type>Observing</type>
        <title>{{name of the observering activity}}</title>
        <associatedParty>{{description/reference to the agent(s) associated with the activity}}</associatedParty>
        <startTime>{{start datetime of the activity}}</startTime>
        <endTime>{{end datetime of the activity}}</endTime>
        <instrumentation>{{Reference to the device(s) used in the observing activity}}</instrumentation>
        <permit>{{Reference to the permit associated with the observing activity}}</permit>
        <dataResult>{{Description/reference to the result(s) from the observing activity}}</dataResult>
        <protocol>{{Reference to the protocol associated with the observing activity}}</protocol>
    </methodStep>
</methods>
