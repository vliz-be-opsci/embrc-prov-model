<!-- Permit entity --> 
<permit id=”{{an identifier/code of the permit}}”>
    <title>{{Name of the permit}}</title>
    <scope>{{A short description of the scope of the permit}}</scope>
    <dueDiligence>{{A description of the due diligence of the permit}}</dueDiligence>
    <validIn>{{reference to the Location where the permit is valid}}</validIn>
    <validFor>{{datetime range during which the permit is valid}}</validFor>
    <validFrom>{{Start datetime from when the permit is valid}}</validFrom>
    <validUntil>{{End datetime until the permit is valid}}</validUntil>
    <associatedParty>{{description/reference to the associatedParty(s) associated with this permit}}</associatedParty>
    <issuer>{{description/reference to the organization responsible for issuing the permit}}</issuer>
    <associatedSample>{{reference to the sample(s) associated with this permit}}</associatedSample>
</permit>

